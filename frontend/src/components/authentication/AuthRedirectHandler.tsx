'use client';

/**
 * Flujo legado desactivado: los usuarios autenticados con Google ya no
 * deben pasar por post-register para acceder a la aplicación.
 */
export default function AuthRedirectHandler() {
  return null;
}